<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- =========================================================
         MAIN APP THEME
    ========================================================== -->

    <style
        name="AppTheme"
        parent="Theme.AppCompat.DayNight.NoActionBar">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowBackground">#03040A</item>

        <item name="android:statusBarColor">#03040A</item>
        <item name="android:navigationBarColor">#03040A</item>

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>


    <!-- =========================================================
         APP WITHOUT ACTION BAR
    ========================================================== -->

    <style
        name="AppTheme.NoActionBar"
        parent="Theme.AppCompat.DayNight.NoActionBar">

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowBackground">#03040A</item>

        <item name="android:statusBarColor">#03040A</item>
        <item name="android:navigationBarColor">#03040A</item>

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

    </style>


    <!-- =========================================================
         PGAME SPLASH THEME
    ========================================================== -->

    <style
        name="AppTheme.NoActionBarLaunch"
        parent="Theme.SplashScreen">

        <!-- Android 12+ background -->
        <item name="windowSplashScreenBackground">#03040A</item>

        <!-- Central PGame neon emblem -->
        <item name="windowSplashScreenAnimatedIcon">
            @drawable/splash_icon
        </item>

        <!-- Theme after splash -->
        <item name="postSplashScreenTheme">
            @style/AppTheme.NoActionBar
        </item>

        <!-- Backup background -->
        <item name="android:windowBackground">
            @drawable/splash
        </item>

        <item name="android:statusBarColor">
            #03040A
        </item>

        <item name="android:navigationBarColor">
            #03040A
        </item>

        <item name="android:windowLightStatusBar">
            false
        </item>

        <item name="android:windowLightNavigationBar">
            false
        </item>

    </style>

</resources>